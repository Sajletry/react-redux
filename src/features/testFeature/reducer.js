export const testActions = {

};

const initialState = {
    testIncrementValue: 0,
};

export function testReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
};



